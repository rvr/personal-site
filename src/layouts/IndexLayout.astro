---
import DefaultLayout from './DefaultLayout.astro';
import PageHeader from '@components/PageHeader.astro';
const { siteSection, pageTitle, pageHeading, intro } = Astro.props;
---

<DefaultLayout siteSection={siteSection} pageTitle={pageTitle}>
    
    <PageHeader title={pageHeading}, intro={intro} />

    <slot />

</DefaultLayout>